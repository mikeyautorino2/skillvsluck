# Dispersion

For each league, each team, we calculate the `win_rate` of this team and the SD of win rate (`std_win_rate`) for each season.

Then we rank the sd from high to low, as stored in the column `sd_rank`.